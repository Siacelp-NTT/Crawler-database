# VietnamWorks Transformation Rules - TEMPLATE

platform_name: "VietnamWorks"
platform_id: 6

salary:
  method: "manual"
  patterns: []                  # TODO: Fill when you have VietnamWorks data
  ai_fallback:
    enabled: true
  default_currency: "VND"

experience_level:
  method: "manual"
  mappings: {}
  ai_fallback:
    enabled: true
  default: "Entry"

work_type:
  method: "manual"
  mappings: {}
  default: "Full-time"

location:
  method: "manual"
  city_mappings: {}
  default_country: 'VNM'

description:
  method: "ai"
  ai_processing:
    enabled: true

date:
  method: "manual"
  formats: []
  ai_fallback:
    enabled: true

quality_checks:
  required_fields:
    - job_title
    - url